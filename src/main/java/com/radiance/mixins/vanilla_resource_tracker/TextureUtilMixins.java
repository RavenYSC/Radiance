package com.radiance.mixins.vanilla_resource_tracker;

/**
 * Stub: TextureUtil.prepareImage() was removed in 1.21.11.
 * The texture system now uses GpuTexture objects.
 * Texture tracking will need to be reimplemented via GpuTexture hooks.
 */
// Disabled: TextureUtil.prepareImage no longer exists in 1.21.11
// @Mixin(TextureUtil.class)
public abstract class TextureUtilMixins {
}
